{% extends 'AppBundle::layout.html.twig' %}


{% block title %}Brunch{% endblock %}

{% block body %}

<div class="container">
    <div>
        <ol id="carousel_images">
        {% for image in place.images %}
            <li><img src="{{image.imageUrl }}"></li>
        {% endfor %}
        </ol>
        <div>
            <h3>{{ place.name }}</h3>
            <p>{{ place.address ~ ' ' ~ place.postalCode ~ ' ' ~ place.city}}</p>
        </div>
    </div>
</div>
<div class="container">
    {% include 'FOSCommentBundle:Thread:async.html.twig' with {'id': 35} %}
</div>

{% endblock %}